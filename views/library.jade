extends layout

block content
  .page-header
    h3 Library Page
  div
    ul
      li #{lib.name}
      li #{lib.story}
      li
        a(href='https://www.google.com/maps/place/+#{lib.street}+#{lib.city}+#{lib.state}+#{lib.zip}') #{lib.street} #{lib.city} #{lib.state} #{lib.zip}
      li #{lib.email}
      li #{lib.steward_name}

